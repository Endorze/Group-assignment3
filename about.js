//I don't really know how to do it but istead of adding a new div for every person, we could add the content in JS?

//Idea:

function Person(name, aboutText) {
    this.name = name;
    this.aboutText = aboutText;
}

let emelie = new Person("Emelie B. Silfverberg", "text");
